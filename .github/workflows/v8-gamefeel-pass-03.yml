name: V8 game-feel pass 03

on:
  push:
    branches: [agent/v8-me-ui-pass-01]
    paths:
      - .github/workflows/v8-gamefeel-pass-03.yml
      - .github/v8-gamefeel-pass-03.py

permissions:
  contents: write

jobs:
  apply-and-verify:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/v8-me-ui-pass-01
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Apply game-feel pass
        run: |
          python3 .github/v8-gamefeel-pass-03.py
          git diff --check

      - run: npm ci

      - name: TypeScript
        run: npx tsc --noEmit

      - name: Lint changed UI
        run: npx eslint src/components/match-v8/V8CalibrationLab.tsx tests/v8-match-lab.spec.ts

      - name: Build static export
        env:
          NEXT_PUBLIC_BASE_PATH: ""
        run: npm run build

      - name: Install Chromium
        run: npx playwright install --with-deps chromium

      - name: Serve static export
        run: |
          python3 -m http.server 3001 --directory out > /tmp/kickoff-clash-http.log 2>&1 &
          for attempt in {1..30}; do
            if curl --fail --silent http://127.0.0.1:3001/lab/match-v8/ > /dev/null; then
              exit 0
            fi
            sleep 1
          done
          cat /tmp/kickoff-clash-http.log
          exit 1

      - name: V8 mobile browser suite
        run: npx playwright test tests/v8-match-lab.spec.ts --project=chromium

      - name: Capture scoring moment
        if: always()
        run: |
          node <<'NODE'
          const { chromium } = require('@playwright/test');
          (async () => {
            const browser = await chromium.launch({ headless: true });
            const page = await browser.newPage({ viewport: { width: 390, height: 844 } });
            await page.goto('http://127.0.0.1:3001/lab/match-v8/', { waitUntil: 'networkidle' });
            await page.getByTestId('player-card-bremner').click();
            await page.locator('.v8-zone').nth(1).click();
            await page.getByRole('button', { name: 'END PERIOD' }).click();
            await page.waitForTimeout(1450);
            await page.screenshot({ path: 'v8-gamefeel-pass-03.png' });
            await browser.close();
          })().catch((error) => { console.error(error); process.exit(1); });
          NODE

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: v8-gamefeel-pass-03-mobile
          path: v8-gamefeel-pass-03.png

      - name: Commit verified game-feel pass
        shell: bash
        run: |
          git rm .github/v8-gamefeel-pass-03.py
          git rm .github/workflows/v8-gamefeel-pass-03.yml
          git config user.name "Neutral Working"
          git config user.email "13trianglez@gmail.com"
          git add src/components/match-v8/V8CalibrationLab.tsx src/components/match-v8/v8lab.css tests/v8-match-lab.spec.ts
          git commit -m "Add V8 period-resolution game feel"
          git push origin HEAD:agent/v8-me-ui-pass-01
