import ManagerCardLab from '@/components/manager-cards/ManagerCardLab';

/** Unlinked mobile grooming route for the V1 manager-card family. */
export default function ManagerCardsLabPage() {
  return <ManagerCardLab />;
}
