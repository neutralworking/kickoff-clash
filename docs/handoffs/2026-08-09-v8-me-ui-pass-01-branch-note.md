# V8 ME UI branch isolation

`agent/v8-me-ui-pass-01` is UI-only and is based on the current V8 balance branch.

Balance ownership remains on `claude/generated-tactical-window-6dxfay` / PR #106.

Do not merge balance work into this branch opportunistically. Reconcile the two streams deliberately after the UI pass is accepted.
